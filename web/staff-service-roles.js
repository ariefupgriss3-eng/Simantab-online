/* SIMANTAB_STAFF_SERVICE_ROLES_V1 */
/* SIMANTAB_PENSION_WORKFLOW_V1 */
(async()=>{
const wait=ms=>new Promise(r=>setTimeout(r,ms));
for(let i=0;i<120&&(!window.__simantabSb||!window.showTab);i++)await wait(50);
const sb=window.__simantabSb,$=id=>document.getElementById(id);if(!sb)return;
const STAFF_ROLES={
 STAFF_KGB:'Staf Gaji Berkala',
 STAFF_KP_EKIN:'Staf KP/PAK/Jabfung/SKP-PAK',
 STAFF_PROMOSI:'Staf Promosi Karir',
 STAFF_ARSIP:'Staf Arsip & Aset',
 STAFF_SKP:'Staf SKP',
 STAFF_PENSIUN:'Staf Pensiun/Berhenti',
 STAFF_CUTI:'Staf Izin Cuti/Sakit/Umroh',
 STAFF_SPJ_SIMTENDIK:'Staf Simtendik',
 STAFF_USUL_SK:'Staf Usul Penerbitan SK'
};
const expandedDinas=['SUPER_ADMIN','KEPALA_DINAS','KABID','KASI_SD','KASI_SMP','SUBKOOR_TK','STAFF_DINAS','STAFF_TPG',...Object.keys(STAFF_ROLES),'PENGAWAS'];
const isExpandedStaff=()=>Object.hasOwn(STAFF_ROLES,window.__simantabProfile?.role||'');
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
function addCreatorOptions(){const sel=$('newDinasRole');if(!sel)return;for(const [value,label] of Object.entries(STAFF_ROLES)){if(sel.querySelector(`option[value="${value}"]`))continue;const o=document.createElement('option');o.value=value;o.textContent=label;const peng=sel.querySelector('option[value="PENGAWAS"]');if(peng)sel.insertBefore(o,peng);else sel.appendChild(o)}}
function credentialCard(){if(!isExpandedStaff()||!$('profileBody')||$('serviceStaffCredentialCard')||$('dinasCredentialCard'))return;const p=window.__simantabProfile||{},wrap=document.createElement('div');wrap.id='serviceStaffCredentialCard';wrap.className='card';wrap.style.marginTop='12px';wrap.innerHTML=`<h3 style="margin-top:0">🔐 Username & Password Login</h3>${p.must_change_password?'<div class="notice" style="margin-bottom:10px"><b>Login pertama:</b> password sementara wajib diganti sebelum menggunakan menu lain.</div>':''}<div class="grid"><div class="field s6"><label>Username</label><input id="staffLoginUsername" value="${esc(p.login_username||'')}" placeholder="username"></div><div class="field s6"><label>Password baru</label><input id="staffNewPassword" type="password" autocomplete="new-password" placeholder="Minimal 8 karakter"></div><div class="field s6"><label>Ulangi password baru</label><input id="staffConfirmPassword" type="password" autocomplete="new-password" placeholder="Ulangi password"></div></div><button class="btn primary" onclick="saveServiceStaffCredentials()">💾 Simpan Username / Password</button><div id="staffCredentialMsg"></div>`;$('profileBody').appendChild(wrap)}
window.saveServiceStaffCredentials=async()=>{if(!isExpandedStaff())return;const username=$('staffLoginUsername').value.trim().toLowerCase(),password=$('staffNewPassword').value,confirm=$('staffConfirmPassword').value,msg=$('staffCredentialMsg'),p=window.__simantabProfile||{};if(!/^[A-Za-z0-9._-]{3,32}$/.test(username)){msg.className='err';msg.textContent='Username harus 3–32 karakter.';return}if(password&&password.length<8){msg.className='err';msg.textContent='Password baru minimal 8 karakter.';return}if(p.must_change_password&&!password){msg.className='err';msg.textContent='Pada login pertama, password baru wajib diisi.';return}if(password!==confirm){msg.className='err';msg.textContent='Ulangi password tidak sama.';return}msg.className='small';msg.textContent='Menyimpan...';const {data:{session}}=await sb.auth.getSession();const {data,error}=await sb.functions.invoke('simantab-account-settings',{body:{username,password:password||undefined},headers:session?.access_token?{Authorization:`Bearer ${session.access_token}`}:{}});if(error||!data?.ok){let detail=data?.error||'';try{detail=detail||(await error?.context?.clone?.().json())?.error||''}catch(_){}msg.className='err';msg.textContent=detail||error?.message||'Gagal menyimpan.';return}window.__simantabProfile={...p,...data.profile};msg.className='okmsg';msg.textContent='Username/password berhasil diperbarui.';setTimeout(()=>location.reload(),700)};
const oldShow=window.showTab;window.showTab=async id=>{const p=window.__simantabProfile;if(p?.must_change_password&&expandedDinas.includes(p.role)&&id!=='profile')id='profile';await oldShow(id);await wait(40);if(id==='users')addCreatorOptions();if(id==='profile')credentialCard()};
for(let i=0;i<80&&!window.__simantabProfile;i++)await wait(100);
if($('newDinasRole'))addCreatorOptions();
if(isExpandedStaff()){if(window.__simantabProfile?.must_change_password)await window.showTab('profile');else if(document.querySelector('.section.active')?.id==='profile')credentialCard()}

// ===== PENSIUN: SIMPEG reference + SIMANTAB issuance workflow =====
const FILE_LIMIT=512000;
const p=()=>window.__simantabProfile||{};
const isDinas=()=>expandedDinas.includes(p().role||'');
const canProcessPension=()=>['SUPER_ADMIN','KABID','STAFF_PENSIUN'].includes(p().role||'');
const PENSION_REF=[
 {type:'PENSIUN_BUP_SIMPEG',title:'Pensiun BUP',desc:'Batas Usia Pensiun'},
 {type:'PENSIUN_JANDA_DUDA_SIMPEG',title:'Pensiun Janda/Duda',desc:'Pensiun karena PNS meninggal dunia'},
 {type:'PENSIUN_APS_SIMPEG',title:'Pensiun APS',desc:'Atas Permintaan Sendiri'}
];
const PROCESS_TYPES={
 PENSIUN_HUDIS_PIDANA:{title:'Surat Pernyataan Hudis/Pidana',subtitle:'Penerbitan oleh Dinas melalui SIMANTAB',outputs:['HASIL_HUDIS','HASIL_PIDANA']},
 PENSIUN_SKMD:{title:'SKMD',subtitle:'Surat Keterangan Meninggal Dunia',outputs:['HASIL_SKMD']}
};
const coordinatorFor=level=>level==='TK'?'SUBKOOR_TK':level==='SD'?'KASI_SD':'KASI_SMP';
async function pensionReqs(type){const {data,error}=await sb.from('service_requirements').select('id,code,label,is_required,sort_order,description').eq('service_type',type).eq('is_active',true).order('sort_order');if(error)throw error;return data||[]}
function pensionInfo(){return `<div class="info" style="margin-bottom:12px"><b>Alur resmi:</b> Pensiun BUP, Janda/Duda, dan APS diproses langsung oleh <b>Operator Korwil (TK/SD)</b> atau <b>Operator SMP (SMP)</b> di <b>SIMPEG</b>. Staf Pensiun Dinas tidak menginput ulang usulan tersebut di SIMANTAB; tugasnya <b>memantau dan approve melalui SIMPEG</b>.<br><br><b>SIMANTAB digunakan Dinas</b> untuk memproses usul penerbitan Surat Pernyataan Hudis/Pidana dan SKMD.</div>`}
function showNewSubmissionSection(){document.querySelectorAll('.section').forEach(x=>x.classList.toggle('active',x.id==='newSubmission'));document.querySelectorAll('.navbtn').forEach(x=>x.classList.remove('active'));$('sidebar')?.classList.remove('open')}
window.__openPensionHub=async()=>{if(window.__simantabSchoolAccess?.isPrivate){alert('Akses sekolah swasta mengikuti pembatasan layanan yang telah ditetapkan.');return window.showTab('dashboard')}showNewSubmissionSection();const section=$('newSubmission');if(!section)return;section.innerHTML='<div class="card">Memuat modul pensiun...</div>';const processButtons=canProcessPension()?`<div class="servicegrid" style="margin-top:12px"><div class="service"><div class="emoji">📄</div><h3>Surat Hudis/Pidana</h3><p>Pengantar + nominatif, SK KP terakhir, dan SK Jabatan/Penempatan. Diproses oleh Dinas melalui SIMANTAB.</p><button class="btn primary" onclick="__openPensionLetterForm('PENSIUN_HUDIS_PIDANA')">Proses di SIMANTAB</button></div><div class="service"><div class="emoji">🕊️</div><h3>SKMD</h3><p>Pengantar + nominatif, bukti kematian, SK KP terakhir, dan SK Jabatan/Penempatan.</p><button class="btn primary" onclick="__openPensionLetterForm('PENSIUN_SKMD')">Proses di SIMANTAB</button></div></div>`:`<div class="notice" style="margin-top:12px">Usul Hudis/Pidana dan SKMD diproses oleh <b>Staf Pensiun Dinas</b> melalui SIMANTAB. Operator Korwil/SMP menyampaikan pengantar dan dokumen pendukung ke Dinas.</div>`;section.innerHTML=`<div class="head"><div><h2>Proses Pensiun PNS</h2><p>Pemisahan proses SIMPEG dan SIMANTAB.</p></div><button class="btn soft" onclick="showTab('services')">← Kembali</button></div>${pensionInfo()}<div class="servicegrid">${PENSION_REF.map(x=>`<div class="service"><div class="emoji">🏁</div><h3>${x.title}</h3><p>${x.desc}. Pengajuan dilakukan di SIMPEG, bukan SIMANTAB.</p><button class="btn soft" onclick="__showPensionChecklist('${x.type}','${x.title}')">Lihat Checklist SIMPEG</button></div>`).join('')}</div>${processButtons}`};
window.__showPensionChecklist=async(type,title)=>{showNewSubmissionSection();const section=$('newSubmission');if(!section)return;section.innerHTML='<div class="card">Memuat checklist...</div>';try{const reqs=await pensionReqs(type);section.innerHTML=`<div class="head"><div><h2>${esc(title)}</h2><p>Checklist referensi untuk pengajuan di SIMPEG.</p></div><button class="btn soft" onclick="__openPensionHub()">← Kembali</button></div>${pensionInfo()}<div class="card"><h3 style="margin-top:0">Berkas Persyaratan</h3><div class="tablewrap"><table><thead><tr><th>No.</th><th>Dokumen</th></tr></thead><tbody>${reqs.map(r=>`<tr><td>${r.sort_order}</td><td><b>${esc(r.label)}</b>${r.description?`<div class="small">${esc(r.description)}</div>`:''}</td></tr>`).join('')}</tbody></table></div><div class="notice" style="margin-top:12px"><b>Tidak ada tombol unggah di SIMANTAB.</b> Operator Korwil/SMP menyiapkan dan mengunggah berkas tersebut langsung ke SIMPEG. Admin/Staf Dinas memantau dan approve proses usul pensiun melalui SIMPEG.</div></div>`}catch(e){section.innerHTML=`<div class="card err">${esc(e.message||e)}</div>`}};
window.__openPensionLetterForm=async type=>{if(!canProcessPension()){alert('Proses ini hanya untuk Staf Pensiun Dinas/Kabid/Super Admin.');return}const cfg=PROCESS_TYPES[type];if(!cfg)return;showNewSubmissionSection();const section=$('newSubmission');section.innerHTML='<div class="card">Memuat form...</div>';try{const reqs=await pensionReqs(type),inputs=reqs.filter(r=>!r.code.startsWith('HASIL_'));section.innerHTML=`<div class="head"><div><h2>${esc(cfg.title)}</h2><p>${esc(cfg.subtitle)} • maksimal 500 KB per berkas.</p></div><button class="btn soft" onclick="__openPensionHub()">← Kembali</button></div><div class="card" style="margin-bottom:12px"><div class="grid"><div class="field s6"><label>Nama PNS</label><input id="penName" placeholder="Nama lengkap PNS"></div><div class="field s6"><label>NIP</label><input id="penNip" inputmode="numeric" placeholder="NIP"></div><div class="field s6"><label>Unit/Sekolah</label><input id="penUnit" placeholder="Nama sekolah/unit kerja"></div><div class="field s3"><label>Jenjang</label><select id="penLevel"><option value="TK">TK</option><option value="SD" selected>SD</option><option value="SMP">SMP</option></select></div><div class="field s3"><label>Sumber Pengantar</label><select id="penSource"><option value="KORWIL">Korwil</option><option value="SMP">Operator SMP</option></select></div><div class="field s6"><label>Nomor Pengantar</label><input id="penLetterNo" placeholder="Nomor surat pengantar"></div><div class="field s6"><label>Tanggal Pengantar</label><input id="penLetterDate" type="date"></div><div class="field s12"><label>Catatan</label><textarea id="penNotes" placeholder="Catatan proses jika diperlukan"></textarea></div></div></div><div class="card" style="margin-bottom:12px"><h3 style="margin-top:0">Dokumen Masuk</h3>${inputs.map(r=>`<div class="field"><label>${r.sort_order}. ${esc(r.label)} ${r.is_required?'<b style="color:#b42318">*</b>':''}</label><input class="pensionReqFile" data-code="${esc(r.code)}" data-required="${r.is_required?'1':'0'}" type="file" accept=".pdf,.jpg,.jpeg,.png,application/pdf,image/jpeg,image/png"><div class="small">${esc(r.description||'')} • Maksimal 500 KB.</div></div>`).join('')}</div><div class="card"><button id="penSendBtn" class="btn primary" onclick="__submitPensionLetter('${type}')">💾 Simpan & Mulai Proses</button><div id="penMsg"></div></div>`}catch(e){section.innerHTML=`<div class="card err">${esc(e.message||e)}</div>`}};
window.__submitPensionLetter=async type=>{const cfg=PROCESS_TYPES[type],msg=$('penMsg'),btn=$('penSendBtn');let sub=null;try{const name=$('penName').value.trim(),nip=$('penNip').value.trim(),unit=$('penUnit').value.trim(),level=$('penLevel').value,source=$('penSource').value,letterNo=$('penLetterNo').value.trim(),letterDate=$('penLetterDate').value,notes=$('penNotes').value.trim();if(!name||!nip||!unit||!letterNo||!letterDate)throw new Error('Nama PNS, NIP, unit/sekolah, nomor dan tanggal pengantar wajib diisi.');const files=[...document.querySelectorAll('.pensionReqFile')];for(const input of files){const f=input.files?.[0];if(input.dataset.required==='1'&&!f)throw new Error('Dokumen wajib belum lengkap.');if(f&&(f.size>FILE_LIMIT||!['application/pdf','image/jpeg','image/png'].includes(f.type)))throw new Error(`${f.name}: maksimal 500 KB dan format PDF/JPG/PNG.`)}btn.disabled=true;msg.className='small';msg.textContent='Menyimpan proses pensiun...';const description=`Nama PNS: ${name}\nNIP: ${nip}\nUnit/Sekolah: ${unit}\nJenjang: ${level}\nSumber Pengantar: ${source}\nNomor Pengantar: ${letterNo}\nTanggal Pengantar: ${letterDate}\nCatatan: ${notes||'-'}`;const {data,error}=await sb.from('submissions').insert({user_id:p().id,service_type:type,title:`${cfg.title} — ${name}`,description,coordinator_role:coordinatorFor(level),assigned_role:'STAFF_PENSIUN'}).select('*').single();if(error)throw error;sub=data;for(const input of files){const f=input.files?.[0];if(!f)continue;const safe=f.name.normalize('NFKD').replace(/[^\w.\-]+/g,'_').slice(-100),path=`${p().id}/${sub.id}/${crypto.randomUUID()}_${safe}`;const {error:upErr}=await sb.storage.from('simantab-documents').upload(path,f,{contentType:f.type,upsert:false});if(upErr)throw upErr;const {error:metaErr}=await sb.from('submission_files').insert({submission_id:sub.id,user_id:p().id,storage_path:path,file_name:f.name,file_size:f.size,mime_type:f.type,requirement_code:input.dataset.code});if(metaErr)throw metaErr}msg.className='okmsg';msg.textContent='Proses berhasil disimpan di SIMANTAB dan masuk antrean Staf Pensiun.';setTimeout(()=>window.__openPensionProcess(sub.id),500)}catch(e){msg.className='err';msg.textContent=e.message||String(e)}finally{if(btn)btn.disabled=false}};
async function pensionBundle(id){const [{data:sub,error:e1},{data:files,error:e2}]=await Promise.all([sb.from('submissions').select('*').eq('id',id).single(),sb.from('submission_files').select('*').eq('submission_id',id).order('created_at')]);if(e1||e2)throw(e1||e2);const reqs=await pensionReqs(sub.service_type);return{sub,files:files||[],reqs}}
async function openStored(path){const {data,error}=await sb.storage.from('simantab-documents').createSignedUrl(path,120);if(error)alert(error.message);else window.open(data.signedUrl,'_blank','noopener')}
window.__openPensionStored=encoded=>openStored(decodeURIComponent(encoded));
function ensurePensionModal(){let m=$('pensionProcessModal');if(m)return m;m=document.createElement('div');m.id='pensionProcessModal';m.className='sigmodal hidden';m.innerHTML='<div class="sigbox" style="width:min(1050px,100%);max-height:92vh;overflow:auto"><div id="pensionProcessContent"></div></div>';document.body.appendChild(m);return m}
window.__closePensionProcess=()=>ensurePensionModal().classList.add('hidden');
window.__openPensionProcess=async id=>{const m=ensurePensionModal(),c=$('pensionProcessContent');m.classList.remove('hidden');c.innerHTML='<div class="small">Memuat proses...</div>';try{const b=await pensionBundle(id),cfg=PROCESS_TYPES[b.sub.service_type];if(!cfg)throw new Error('Jenis proses pensiun tidak dikenali.');const latest={};for(const f of b.files)if(f.requirement_code)latest[f.requirement_code]=f;const outputs=b.reqs.filter(r=>r.code.startsWith('HASIL_'));c.innerHTML=`<div class="head"><div><h2>${esc(cfg.title)}</h2><p>${esc(b.sub.title)} • <span class="status st-${esc(b.sub.status)}">${esc(b.sub.status)}</span></p></div><button class="btn soft" onclick="__closePensionProcess()">Tutup</button></div><div class="card" style="margin-bottom:12px"><h3 style="margin-top:0">Data Usul</h3><div style="white-space:pre-line" class="small">${esc(b.sub.description||'-')}</div></div><div class="card" style="margin-bottom:12px"><h3 style="margin-top:0">Dokumen</h3><div class="tablewrap"><table><thead><tr><th>No.</th><th>Dokumen</th><th>Berkas</th></tr></thead><tbody>${b.reqs.map(r=>{const f=latest[r.code];return `<tr><td>${r.sort_order}</td><td><b>${esc(r.label)}</b><div class="small">${esc(r.description||'')}</div></td><td>${f?`<button class="btn soft" onclick="__openPensionStored('${encodeURIComponent(f.storage_path)}')">📎 ${esc(f.file_name)}</button>`:(r.code.startsWith('HASIL_')?'<span class="status st-VERIFYING">Belum diterbitkan</span>':'<span class="status st-REVISION">Belum ada</span>')}</td></tr>`}).join('')}</tbody></table></div></div>${canProcessPension()?`<div class="card" style="margin-bottom:12px"><h3 style="margin-top:0">Unggah Hasil Surat Dinas</h3>${outputs.map(r=>`<div class="field"><label>${esc(r.label)}</label>${latest[r.code]?`<div class="okmsg">Sudah ada: ${esc(latest[r.code].file_name)}</div>`:`<input class="pensionOutputFile" data-code="${esc(r.code)}" type="file" accept=".pdf,.jpg,.jpeg,.png,application/pdf,image/jpeg,image/png"><div class="small">PDF/JPG/PNG maksimal 500 KB.</div>`}</div>`).join('')}<button class="btn primary" onclick="__savePensionOutputs('${id}','${b.sub.service_type}')">⬆ Simpan Hasil Surat</button></div><div class="card"><h3 style="margin-top:0">Status Proses</h3><div style="display:flex;gap:8px;flex-wrap:wrap"><button class="btn soft" onclick="__setPensionStatus('${id}','VERIFYING','${b.sub.service_type}')">Sedang Diproses</button><button class="btn danger" onclick="__setPensionStatus('${id}','REVISION','${b.sub.service_type}')">Perlu Kelengkapan</button><button class="btn success" onclick="__setPensionStatus('${id}','APPROVED','${b.sub.service_type}')">Surat Terbit</button><button class="btn primary" onclick="__setPensionStatus('${id}','COMPLETED','${b.sub.service_type}')">Selesai / Siap Diambil</button></div><div class="small" style="margin-top:8px">Setelah surat selesai, Operator Korwil/SMP mengambil/scan PDF dan mengunggahnya ke SIMPEG bersama berkas pensiun lainnya.</div></div>`:'<div class="notice">Akun ini dapat memantau proses. Pemrosesan dan unggah hasil dilakukan oleh Staf Pensiun Dinas.</div>'}`;}catch(e){c.innerHTML=`<div class="err">${esc(e.message||e)}</div><button class="btn soft" onclick="__closePensionProcess()">Tutup</button>`}};
window.__savePensionOutputs=async(id,type)=>{try{const inputs=[...document.querySelectorAll('.pensionOutputFile')];let count=0;for(const input of inputs){const f=input.files?.[0];if(!f)continue;if(f.size>FILE_LIMIT||!['application/pdf','image/jpeg','image/png'].includes(f.type))throw new Error(`${f.name}: maksimal 500 KB dan format PDF/JPG/PNG.`);const safe=f.name.normalize('NFKD').replace(/[^\w.\-]+/g,'_').slice(-100),path=`${p().id}/${id}/${crypto.randomUUID()}_${safe}`;const {error:upErr}=await sb.storage.from('simantab-documents').upload(path,f,{contentType:f.type,upsert:false});if(upErr)throw upErr;const {error:metaErr}=await sb.from('submission_files').insert({submission_id:id,user_id:p().id,storage_path:path,file_name:f.name,file_size:f.size,mime_type:f.type,requirement_code:input.dataset.code});if(metaErr)throw metaErr;count++}if(!count){alert('Pilih berkas hasil surat terlebih dahulu.');return}alert('Hasil surat berhasil disimpan.');await window.__openPensionProcess(id)}catch(e){alert(e.message||String(e))}};
window.__setPensionStatus=async(id,status,type)=>{try{if(status==='COMPLETED'){const cfg=PROCESS_TYPES[type],{data,error}=await sb.from('submission_files').select('requirement_code').eq('submission_id',id);if(error)throw error;const codes=new Set((data||[]).map(x=>x.requirement_code));const missing=cfg.outputs.filter(x=>!codes.has(x));if(missing.length)throw new Error('Status Selesai belum dapat dipilih karena hasil surat Dinas belum lengkap.')}const {error}=await sb.from('submissions').update({status}).eq('id',id);if(error)throw error;await window.__openPensionProcess(id)}catch(e){alert(e.message||String(e))}};
function pensionServiceShortcut(){if(!isDinas()||!$('servicesBody')||$('pensionDinasShortcut'))return;const d=document.createElement('div');d.id='pensionDinasShortcut';d.className='card';d.style.marginBottom='12px';d.innerHTML=`<div class="head" style="margin-bottom:0"><div><h3 style="margin:0">🏁 Proses Pensiun PNS</h3><div class="small">BUP/Janda-Duda/APS melalui SIMPEG; Hudis/Pidana dan SKMD melalui SIMANTAB.</div></div><button class="btn primary" onclick="__openPensionHub()">Buka Modul Pensiun</button></div>`;$('servicesBody').prepend(d)}
function pensionProcessButtons(){if(!isDinas())return;for(const rootId of ['servicesBody','monitoringBody']){const root=$(rootId);if(!root)continue;root.querySelectorAll('tbody tr').forEach(tr=>{const tds=tr.querySelectorAll('td'),type=tds[1]?.textContent.trim();if(!PROCESS_TYPES[type]||tr.querySelector('.pension-process-btn'))return;const sel=tr.querySelector('select[onchange*="updateStatus"]');if(!sel)return;const match=(sel.getAttribute('onchange')||'').match(/updateStatus\('([^']+)'/);if(!match)return;const b=document.createElement('button');b.className='btn primary pension-process-btn';b.style.marginTop='5px';b.textContent='⚙ Proses Pensiun';b.onclick=()=>window.__openPensionProcess(match[1]);sel.parentElement.appendChild(b)})}}
function pensionNav(){if(!isDinas()||!$('nav')||$('pensionNavBtn'))return;const b=document.createElement('button');b.id='pensionNavBtn';b.className='navbtn';b.innerHTML='<span class="ico">🏁</span>Proses Pensiun';b.onclick=()=>window.__openPensionHub();$('nav').appendChild(b)}
const oldOpenPension=window.openSubmission;if(oldOpenPension&&!window.__pensionOpenWrapped){window.__pensionOpenWrapped=true;window.openSubmission=async(type,title)=>{if(type==='PENSIUN')return window.__openPensionHub();return oldOpenPension(type,title)}}
const pensionShow=window.showTab;window.showTab=async id=>{const r=await pensionShow(id);await wait(70);pensionNav();if(id==='services')pensionServiceShortcut();if(id==='services'||id==='monitoring')pensionProcessButtons();return r};
const obs=new MutationObserver(()=>{pensionNav();pensionProcessButtons()});obs.observe(document.body,{subtree:true,childList:true});
await wait(100);pensionNav();if(document.querySelector('.section.active')?.id==='services')pensionServiceShortcut();
})();