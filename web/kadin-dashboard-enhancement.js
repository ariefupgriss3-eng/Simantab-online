/* SIMANTAB_KEPALA_DINAS_INFOGRAPHIC_V1 */
(async()=>{
const wait=ms=>new Promise(r=>setTimeout(r,ms));
for(let i=0;i<120&&(!window.__simantabSb||!window.showTab);i++)await wait(50);
const sb=window.__simantabSb,$=id=>document.getElementById(id),isKadin=()=>window.__simantabProfile?.role==='KEPALA_DINAS';
if(!sb)return;
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const n=v=>Number(v)||0, fmt=v=>new Intl.NumberFormat('id-ID').format(n(v));
const pct=(a,b)=>b?Math.max(0,Math.min(100,Math.round(a/b*100))):0;
const statusLabel={SUBMITTED:'Diajukan',VERIFYING:'Diverifikasi',REVISION:'Perlu Perbaikan',APPROVED:'Disetujui',COMPLETED:'Selesai',REJECTED:'Ditolak'};
const stageLabel={PENGAJUAN:'Pengajuan',CEK_KELENGKAPAN:'Cek Berkas',VERIF_VALIDASI:'Verifikasi',PERBAIKAN:'Perbaikan',INPUT_VERVALPTK:'Verval PTK',REKOMENDASI:'Rekomendasi',DAPODIK:'Dapodik',SINKRONISASI:'Sinkronisasi',ARSIP:'Arsip'};
let cache=null,cacheAt=0;
function styles(){if($('kadinStyles'))return;const s=document.createElement('style');s.id='kadinStyles';s.textContent=`
.kd-grid{display:grid;grid-template-columns:repeat(12,1fr);gap:13px}.kd-span3{grid-column:span 3}.kd-span4{grid-column:span 4}.kd-span5{grid-column:span 5}.kd-span6{grid-column:span 6}.kd-span7{grid-column:span 7}.kd-span8{grid-column:span 8}.kd-span12{grid-column:span 12}.kd-hero{background:linear-gradient(135deg,#0f3f76,#1767b3);color:#fff;border-radius:20px;padding:20px;box-shadow:0 10px 28px #153d681f}.kd-hero h2{margin:0;font-size:24px}.kd-hero p{margin:6px 0 0;opacity:.9}.kd-kpi{background:#fff;border:1px solid var(--line);border-radius:17px;padding:15px;box-shadow:0 6px 20px rgba(25,54,90,.055)}.kd-kpi-top{display:flex;justify-content:space-between;align-items:flex-start;gap:8px}.kd-icon{font-size:24px}.kd-big{font-size:31px;font-weight:950;margin:4px 0}.kd-sub{font-size:11px;color:var(--muted);line-height:1.5}.kd-panel{background:#fff;border:1px solid var(--line);border-radius:17px;padding:16px;box-shadow:0 6px 20px rgba(25,54,90,.055)}.kd-panel h3{margin:0 0 12px}.kd-row{display:grid;grid-template-columns:74px 1fr 55px;gap:9px;align-items:center;margin:9px 0;font-size:11px}.kd-track{height:9px;border-radius:99px;background:#edf2f7;overflow:hidden}.kd-fill{height:100%;border-radius:99px;background:linear-gradient(90deg,#1767b3,#6aa9df)}.kd-fill.green{background:linear-gradient(90deg,#178354,#6bc49a)}.kd-fill.orange{background:linear-gradient(90deg,#d97706,#f5b95b)}.kd-fill.red{background:linear-gradient(90deg,#b42318,#e4776f)}.kd-donut-wrap{display:flex;gap:18px;align-items:center;flex-wrap:wrap}.kd-donut{width:145px;height:145px;border-radius:50%;display:grid;place-items:center;flex:none}.kd-donut:after{content:'';width:92px;height:92px;background:#fff;border-radius:50%;box-shadow:inset 0 0 0 1px #e4eaf0}.kd-legend{font-size:11px;line-height:1.9}.kd-dot{display:inline-block;width:9px;height:9px;border-radius:50%;margin-right:6px}.kd-priority{display:grid;grid-template-columns:repeat(2,1fr);gap:9px}.kd-alert{border:1px solid var(--line);border-radius:13px;padding:12px;background:#fafcff}.kd-alert b{display:block;font-size:18px;margin-bottom:3px}.kd-nav .navbtn{font-size:12px}.kd-mini-table{width:100%;border-collapse:collapse;font-size:11px}.kd-mini-table th,.kd-mini-table td{padding:8px;border-bottom:1px solid var(--line);text-align:left}.kd-mini-table th{font-size:9px;color:var(--muted);text-transform:uppercase}.kd-pill{display:inline-block;padding:4px 7px;border-radius:999px;background:#edf5ff;color:#175ea7;font-size:9px;font-weight:900}.kd-empty{padding:18px;text-align:center;color:var(--muted);font-size:11px}.kd-actions{display:flex;gap:8px;flex-wrap:wrap}.kd-actions button{border:1px solid var(--line);background:#fff;border-radius:11px;padding:9px 11px;font-weight:800}.kd-section-head{display:flex;justify-content:space-between;gap:10px;align-items:center;margin-bottom:14px}.kd-section-head h2{margin:0}.kd-section-head p{margin:4px 0 0;color:var(--muted);font-size:12px}@media(max-width:980px){.kd-span3,.kd-span4,.kd-span5,.kd-span6,.kd-span7,.kd-span8{grid-column:span 12}.kd-priority{grid-template-columns:1fr}.kd-row{grid-template-columns:65px 1fr 48px}.kd-donut-wrap{justify-content:center}}
`;document.head.appendChild(s)}
function ensureSections(){const main=document.querySelector('main.content');if(!main)return;const defs=[['kadinGtk','Infografis GTK'],['kadinMonitoring','Monitoring Layanan'],['kadinPtk','Usul PTK Baru Swasta'],['kadinActivities','Kegiatan Bidang']];for(const [id] of defs){if($(id))continue;const sec=document.createElement('section');sec.id=id;sec.className='section';main.insertBefore(sec,main.querySelector('.footer'))}}
function nav(){const box=$('nav');if(!box||!isKadin())return;box.classList.add('kd-nav');box.innerHTML=`<div class="navhead">Pimpinan</div>
<button class="navbtn" data-tab="dashboard" onclick="showTab('dashboard')"><span class="ico">▦</span>Dashboard</button>
<button class="navbtn" data-tab="kadinGtk" onclick="showTab('kadinGtk')"><span class="ico">📊</span>Infografis GTK</button>
<button class="navbtn" data-tab="kadinMonitoring" onclick="showTab('kadinMonitoring')"><span class="ico">◫</span>Monitoring Layanan</button>
<button class="navbtn" data-tab="kadinPtk" onclick="showTab('kadinPtk')"><span class="ico">🧑‍🏫</span>PTK Baru Swasta</button>
<button class="navbtn" data-tab="kadinActivities" onclick="showTab('kadinActivities')"><span class="ico">📅</span>Kegiatan Bidang</button>
<div class="navhead">Akun</div>
<button class="navbtn" data-tab="notifications" onclick="showTab('notifications')"><span class="ico">🔔</span>Notifikasi</button>
<button class="navbtn" data-tab="profile" onclick="showTab('profile')"><span class="ico">♙</span>Profil</button>`}
async function getData(force=false){if(!force&&cache&&Date.now()-cacheAt<30000)return cache;const [schoolsR,needsR,subsR,ptkR,actsR,notifR]=await Promise.all([
sb.from('school_master').select('npsn,school_name,school_status,bentuk_pendidikan,jenjang,students,teachers,staff').eq('is_active',true),
sb.from('school_gtk_needs').select('school_npsn,school_name,school_level,position_name,abk,pns,pppk,pppk_pw,asn_total,non_asn_total,gap_riil,gap_data'),
sb.from('submissions').select('id,service_type,title,status,submitted_at,assigned_role').order('submitted_at',{ascending:false}),
sb.from('ptk_swasta_submission_details').select('submission_id,school_npsn,school_name,jenjang,ptk_name,position_name,workflow_stage,created_at,updated_at').order('updated_at',{ascending:false}),
sb.from('field_activities').select('id,activity_name,activity_date,activity_time,place,created_at').order('activity_date',{ascending:false}),
sb.from('notifications').select('*',{count:'exact',head:true}).eq('is_read',false)
]);for(const r of [schoolsR,needsR,subsR,ptkR,actsR,notifR])if(r.error)throw r.error;cache={schools:schoolsR.data||[],needs:needsR.data||[],subs:subsR.data||[],ptk:ptkR.data||[],acts:actsR.data||[],unread:notifR.count||0};cacheAt=Date.now();return cache}
function levelOf(s){return String(s.bentuk_pendidikan||s.jenjang||'').toUpperCase()}
function aggregate(d){const schools=d.schools,needs=d.needs,subs=d.subs,ptk=d.ptk,acts=d.acts;
const school={total:schools.length,TK:0,SD:0,SMP:0,negeri:0,swasta:0,students:0,teachers:0,staff:0};for(const x of schools){const l=levelOf(x);if(l in school)school[l]++;if(String(x.school_status).toUpperCase()==='NEGERI')school.negeri++;if(String(x.school_status).toUpperCase()==='SWASTA')school.swasta++;school.students+=n(x.students);school.teachers+=n(x.teachers);school.staff+=n(x.staff)}
const need={rows:needs.length,schools:new Set(needs.map(x=>x.school_npsn)).size,abk:0,asn:0,non:0,gapRiil:0,gapData:0,shortage:0,surplus:0,pns:0,pppk:0,pw:0,byLevel:{TK:{abk:0,asn:0,gap:0},SD:{abk:0,asn:0,gap:0},SMP:{abk:0,asn:0,gap:0}}};for(const x of needs){need.abk+=n(x.abk);need.asn+=n(x.asn_total);need.non+=n(x.non_asn_total);need.gapRiil+=n(x.gap_riil);need.gapData+=n(x.gap_data);need.shortage+=Math.max(0,n(x.gap_riil));need.surplus+=Math.max(0,-n(x.gap_riil));need.pns+=n(x.pns);need.pppk+=n(x.pppk);need.pw+=n(x.pppk_pw);const l=String(x.school_level||'').toUpperCase();if(need.byLevel[l]){need.byLevel[l].abk+=n(x.abk);need.byLevel[l].asn+=n(x.asn_total);need.byLevel[l].gap+=n(x.gap_riil)}}
const status={};for(const k of Object.keys(statusLabel))status[k]=0;for(const x of subs)status[x.status]=(status[x.status]||0)+1;const active=subs.filter(x=>!['COMPLETED','REJECTED'].includes(x.status)).length;
const service={};for(const x of subs)service[x.service_type]=(service[x.service_type]||0)+1;
const stages={};for(const k of Object.keys(stageLabel))stages[k]=0;const ptkLevel={TK:0,SD:0,SMP:0};for(const x of ptk){stages[x.workflow_stage]=(stages[x.workflow_stage]||0)+1;if(ptkLevel[x.jenjang]!=null)ptkLevel[x.jenjang]++}
const today=new Date().toISOString().slice(0,10);const upcoming=acts.filter(x=>x.activity_date>=today).sort((a,b)=>String(a.activity_date).localeCompare(String(b.activity_date)));return{school,need,status,active,service,stages,ptkLevel,upcoming}}
const kpi=(icon,label,value,sub)=>`<div class="kd-kpi kd-span4"><div class="kd-kpi-top"><div><div class="label">${esc(label)}</div><div class="kd-big">${esc(value)}</div></div><div class="kd-icon">${icon}</div></div><div class="kd-sub">${sub}</div></div>`;
function bar(label,value,max,cls=''){return `<div class="kd-row"><b>${esc(label)}</b><div class="kd-track"><div class="kd-fill ${cls}" style="width:${pct(value,max)}%"></div></div><b>${fmt(value)}</b></div>`}
function levelBars(a){const max=Math.max(1,...Object.values(a.need.byLevel).flatMap(x=>[x.abk,x.asn,Math.max(0,x.gap)]));return ['TK','SD','SMP'].map(l=>{const x=a.need.byLevel[l];return `<div style="margin-bottom:14px"><b>${l}</b>${bar('ABK',x.abk,max)}${bar('ASN',x.asn,max,'green')}${bar('Gap',Math.max(0,x.gap),max,'orange')}</div>`}).join('')}
function donut(a){const vals=[a.need.pns,a.need.pppk,a.need.pw,a.need.non],tot=vals.reduce((x,y)=>x+y,0)||1,p1=vals[0]/tot*100,p2=p1+vals[1]/tot*100,p3=p2+vals[2]/tot*100;return `<div class="kd-donut-wrap"><div class="kd-donut" style="background:conic-gradient(#1767b3 0 ${p1}%,#178354 ${p1}% ${p2}%,#d97706 ${p2}% ${p3}%,#7b8794 ${p3}% 100%)"></div><div class="kd-legend"><div><span class="kd-dot" style="background:#1767b3"></span>PNS <b>${fmt(vals[0])}</b></div><div><span class="kd-dot" style="background:#178354"></span>PPPK <b>${fmt(vals[1])}</b></div><div><span class="kd-dot" style="background:#d97706"></span>PPPK Paruh Waktu <b>${fmt(vals[2])}</b></div><div><span class="kd-dot" style="background:#7b8794"></span>Non-ASN <b>${fmt(vals[3])}</b></div></div></div>`}
function statusBars(a){const max=Math.max(1,...Object.values(a.status));return Object.keys(statusLabel).map((k,i)=>bar(statusLabel[k],a.status[k]||0,max,['','green','orange','orange','green','red'][i]||'')).join('')}
function header(title,desc)=>`<div class="kd-section-head"><div><h2>${esc(title)}</h2><p>${esc(desc)}</p></div><div class="kd-actions"><button onclick="__kadinRefresh()">↻ Segarkan</button><button onclick="window.print()">🖨 Cetak</button></div></div>`;
async function dashboard(force=false){if(!isKadin())return;const b=$('dashboardBody');if(!b)return;$('dashTitle').textContent='Dashboard Kepala Disdikbud';$('dashDesc').textContent='Ringkasan strategis ketenagaan dalam satu layar.';b.innerHTML='<div class="card">Memuat infografis...</div>';try{const d=await getData(force),a=aggregate(d),coverage=pct(a.need.schools,a.school.total),topLevel=Object.entries(a.need.byLevel).sort((x,y)=>y[1].gap-x[1].gap)[0],next=a.upcoming[0];b.innerHTML=`<div class="kd-grid"><div class="kd-hero kd-span12"><h2>Command Center Pimpinan</h2><p>Kondisi sekolah, GTK, layanan, dan isu prioritas Kabupaten Batang.</p></div>
${kpi('🏫','Total Sekolah',fmt(a.school.total),`TK ${fmt(a.school.TK)} • SD ${fmt(a.school.SD)} • SMP ${fmt(a.school.SMP)}<br>Negeri ${fmt(a.school.negeri)} • Swasta ${fmt(a.school.swasta)}`)}
${kpi('👥','GTK Dapodik',fmt(a.school.teachers+a.school.staff),`Guru ${fmt(a.school.teachers)} • Tendik ${fmt(a.school.staff)}<br>Data agregat Master Sekolah`)}
${kpi('◎','Kebutuhan GTK Riil',`${fmt(a.need.schools)} sekolah`,` ${fmt(a.need.rows)} entri jabatan • Gap riil ${fmt(a.need.gapRiil)}<br>Cakupan input ${coverage}%`)}
${kpi('☑','Usulan Aktif',fmt(a.active),`Perbaikan ${fmt(a.status.REVISION)} • Selesai ${fmt(a.status.COMPLETED)}<br>Total usulan ${fmt(d.subs.length)}`)}
${kpi('🧑‍🏫','PTK Baru Swasta',fmt(d.ptk.length),`TK ${fmt(a.ptkLevel.TK)} • SD ${fmt(a.ptkLevel.SD)} • SMP ${fmt(a.ptkLevel.SMP)}`)}
${kpi('🔔','Perhatian Pimpinan',fmt(a.need.shortage+a.status.REVISION),`Kekurangan GTK ${fmt(a.need.shortage)} • Usulan perlu perbaikan ${fmt(a.status.REVISION)}<br>Notifikasi baru ${fmt(d.unread)}`)}
<div class="kd-panel kd-span7"><h3>Kebutuhan GTK per Jenjang</h3>${levelBars(a)}</div><div class="kd-panel kd-span5"><h3>Komposisi GTK dari Input Riil</h3>${donut(a)}</div>
<div class="kd-panel kd-span6"><h3>Status Layanan</h3>${statusBars(a)}</div><div class="kd-panel kd-span6"><h3>Cakupan Input Kebutuhan GTK</h3><div style="font-size:38px;font-weight:950">${coverage}%</div><div class="kd-sub">${fmt(a.need.schools)} dari ${fmt(a.school.total)} sekolah sudah memiliki data kebutuhan.</div><div class="kd-track" style="height:14px;margin-top:12px"><div class="kd-fill green" style="width:${coverage}%"></div></div></div>
<div class="kd-panel kd-span12"><h3>Sorotan Kepala Dinas</h3><div class="kd-priority"><div class="kd-alert"><b>${fmt(a.need.shortage)}</b><span>Kekurangan GTK (akumulasi gap positif)</span></div><div class="kd-alert"><b>${esc(topLevel?.[0]||'-')}</b><span>Jenjang dengan gap agregat tertinggi: ${fmt(topLevel?.[1]?.gap||0)}</span></div><div class="kd-alert"><b>${fmt(Math.max(0,a.school.total-a.need.schools))}</b><span>Sekolah belum memiliki input Kebutuhan GTK Riil</span></div><div class="kd-alert"><b>${next?esc(next.activity_name):'-'}</b><span>${next?`${esc(next.activity_date)} • ${esc(next.place||'-')}`:'Belum ada agenda mendatang'}</span></div></div></div></div>`}catch(e){b.innerHTML=`<div class="card err">Gagal memuat infografis: ${esc(e.message)}</div>`}}
function topShortageRows(d){const m=new Map;for(const x of d.needs){const key=x.school_npsn||x.school_name,v=m.get(key)||{school:x.school_name,npsn:x.school_npsn,gap:0};v.gap+=Math.max(0,n(x.gap_riil));m.set(key,v)}return [...m.values()].filter(x=>x.gap>0).sort((a,b)=>b.gap-a.gap).slice(0,10)}
async function gtkPage(force=false){const b=$('kadinGtk');if(!b)return;b.innerHTML='<div class="card">Memuat...</div>';try{const d=await getData(force),a=aggregate(d),rows=topShortageRows(d),coverage=pct(a.need.schools,a.school.total);b.innerHTML=`${header('Infografis GTK','Ringkasan kebutuhan, ketersediaan, dan gap GTK tanpa form input.') }<div class="kd-grid">${kpi('🎯','ABK Ideal',fmt(a.need.abk),'Akumulasi kebutuhan dari data yang sudah diinput')}${kpi('👤','ASN Aktif',fmt(a.need.asn),'PNS + PPPK + PPPK Paruh Waktu')}${kpi('🧩','Non-ASN',fmt(a.need.non),'Ditampilkan terpisah dari Gap Riil')}${kpi('⚠️','Gap Riil',fmt(a.need.gapRiil),`Kekurangan positif ${fmt(a.need.shortage)} • Kelebihan ${fmt(a.need.surplus)}`)}${kpi('📋','Gap Data',fmt(a.need.gapData),'ABK − (ASN + Non-ASN)')}${kpi('🏫','Cakupan Sekolah',`${coverage}%`,`${fmt(a.need.schools)} dari ${fmt(a.school.total)} sekolah`)}<div class="kd-panel kd-span7"><h3>ABK vs ASN vs Gap per Jenjang</h3>${levelBars(a)}</div><div class="kd-panel kd-span5"><h3>Komposisi GTK</h3>${donut(a)}</div><div class="kd-panel kd-span12"><h3>10 Sekolah dengan Kekurangan Terbesar</h3>${rows.length?`<table class="kd-mini-table"><thead><tr><th>Sekolah</th><th>NPSN</th><th>Gap Riil</th></tr></thead><tbody>${rows.map(x=>`<tr><td><b>${esc(x.school)}</b></td><td>${esc(x.npsn)}</td><td><span class="kd-pill">Kurang ${fmt(x.gap)}</span></td></tr>`).join('')}</tbody></table>`:'<div class="kd-empty">Belum ada gap positif pada data yang tersedia.</div>'}</div></div>`}catch(e){b.innerHTML=`<div class="card err">${esc(e.message)}</div>`}}
async function monitoringPage(force=false){const b=$('kadinMonitoring');if(!b)return;b.innerHTML='<div class="card">Memuat...</div>';try{const d=await getData(force),a=aggregate(d),types=Object.entries(a.service).sort((x,y)=>y[1]-x[1]),max=Math.max(1,...types.map(x=>x[1]));b.innerHTML=`${header('Monitoring Layanan','Pantauan eksekutif tanpa tombol verifikasi atau perubahan status.')}<div class="kd-grid"><div class="kd-panel kd-span6"><h3>Status Seluruh Usulan</h3>${statusBars(a)}</div><div class="kd-panel kd-span6"><h3>Volume per Jenis Layanan</h3>${types.length?types.map(x=>bar(x[0],x[1],max)).join(''):'<div class="kd-empty">Belum ada usulan.</div>'}</div><div class="kd-panel kd-span12"><h3>Usulan Terbaru</h3>${d.subs.length?`<table class="kd-mini-table"><thead><tr><th>Waktu</th><th>Layanan</th><th>Judul</th><th>Status</th></tr></thead><tbody>${d.subs.slice(0,12).map(x=>`<tr><td>${esc(new Date(x.submitted_at).toLocaleDateString('id-ID'))}</td><td>${esc(x.service_type)}</td><td>${esc(x.title)}</td><td><span class="kd-pill">${esc(statusLabel[x.status]||x.status)}</span></td></tr>`).join('')}</tbody></table>`:'<div class="kd-empty">Belum ada usulan.</div>'}</div></div>`}catch(e){b.innerHTML=`<div class="card err">${esc(e.message)}</div>`}}
async function ptkPage(force=false){const b=$('kadinPtk');if(!b)return;b.innerHTML='<div class="card">Memuat...</div>';try{const d=await getData(force),a=aggregate(d),max=Math.max(1,...Object.values(a.stages));b.innerHTML=`${header('Usul PTK Baru Swasta','Pantauan TK, SD, dan SMP swasta dari pengajuan sampai arsip.')}<div class="kd-grid">${kpi('🧸','TK Swasta',fmt(a.ptkLevel.TK),'Jumlah usulan PTK baru TK')}${kpi('🏫','SD Swasta',fmt(a.ptkLevel.SD),'Jumlah usulan PTK baru SD')}${kpi('🎓','SMP Swasta',fmt(a.ptkLevel.SMP),'Jumlah usulan PTK baru SMP')}<div class="kd-panel kd-span12"><h3>Progres Tahapan</h3>${Object.keys(stageLabel).map(k=>bar(stageLabel[k],a.stages[k]||0,max,k==='PERBAIKAN'?'orange':k==='ARSIP'?'green':'')).join('')}</div><div class="kd-panel kd-span12"><h3>Usulan Terbaru</h3>${d.ptk.length?`<table class="kd-mini-table"><thead><tr><th>Sekolah</th><th>Jenjang</th><th>PTK</th><th>Jabatan</th><th>Tahap</th></tr></thead><tbody>${d.ptk.slice(0,12).map(x=>`<tr><td><b>${esc(x.school_name)}</b></td><td>${esc(x.jenjang)}</td><td>${esc(x.ptk_name)}</td><td>${esc(x.position_name)}</td><td><span class="kd-pill">${esc(stageLabel[x.workflow_stage]||x.workflow_stage)}</span></td></tr>`).join('')}</tbody></table>`:'<div class="kd-empty">Belum ada Usul PTK Baru Swasta.</div>'}</div></div>`}catch(e){b.innerHTML=`<div class="card err">${esc(e.message)}</div>`}}
async function activitiesPage(force=false){const b=$('kadinActivities');if(!b)return;b.innerHTML='<div class="card">Memuat...</div>';try{const d=await getData(force),a=aggregate(d),today=new Date().toISOString().slice(0,10),past=d.acts.filter(x=>x.activity_date<today).slice(0,8);b.innerHTML=`${header('Kegiatan Bidang','Agenda pimpinan dan kegiatan bidang dalam tampilan ringkas.')}<div class="kd-grid">${kpi('📅','Agenda Mendatang',fmt(a.upcoming.length),'Kegiatan dengan tanggal hari ini atau sesudahnya')}${kpi('✅','Kegiatan Tercatat',fmt(d.acts.length),'Total kegiatan pada SIMANTAB')}${kpi('🕘','Kegiatan Selesai',fmt(d.acts.filter(x=>x.activity_date<today).length),'Berdasarkan tanggal kegiatan')}<div class="kd-panel kd-span6"><h3>Agenda Mendatang</h3>${a.upcoming.length?a.upcoming.slice(0,8).map(x=>`<div class="kd-alert" style="margin-bottom:8px"><b style="font-size:14px">${esc(x.activity_name)}</b><span>${esc(x.activity_date)} ${x.activity_time?'• '+esc(x.activity_time):''}<br>${esc(x.place||'-')}</span></div>`).join(''):'<div class="kd-empty">Belum ada agenda mendatang.</div>'}</div><div class="kd-panel kd-span6"><h3>Kegiatan Terakhir</h3>${past.length?past.map(x=>`<div class="kd-alert" style="margin-bottom:8px"><b style="font-size:14px">${esc(x.activity_name)}</b><span>${esc(x.activity_date)} • ${esc(x.place||'-')}</span></div>`).join(''):'<div class="kd-empty">Belum ada riwayat kegiatan.</div>'}</div></div>`}catch(e){b.innerHTML=`<div class="card err">${esc(e.message)}</div>`}}
window.__kadinRefresh=async()=>{cache=null;cacheAt=0;const id=document.querySelector('.section.active')?.id||'dashboard';if(id==='dashboard')await dashboard(true);if(id==='kadinGtk')await gtkPage(true);if(id==='kadinMonitoring')await monitoringPage(true);if(id==='kadinPtk')await ptkPage(true);if(id==='kadinActivities')await activitiesPage(true)};
const oldShow=window.showTab;window.showTab=async id=>{await oldShow(id);if(!isKadin())return;nav();if(id==='dashboard')await dashboard();if(id==='kadinGtk')await gtkPage();if(id==='kadinMonitoring')await monitoringPage();if(id==='kadinPtk')await ptkPage();if(id==='kadinActivities')await activitiesPage()};
const oldRefresh=window.refreshAll;window.refreshAll=async()=>{await oldRefresh();if(isKadin())await dashboard(true)};
for(let i=0;i<80&&!window.__simantabProfile;i++)await wait(100);if(isKadin()){styles();ensureSections();nav();await dashboard(true);document.querySelectorAll('.navbtn').forEach(x=>x.classList.toggle('active',x.dataset.tab==='dashboard'))}
})();
