/* SIMANTAB_DINAS_USERNAME_LOGIN_V1 */
(async()=>{
const wait=ms=>new Promise(r=>setTimeout(r,ms));
for(let i=0;i<120&&(!window.__simantabSb||!window.showTab);i++)await wait(50);
const sb=window.__simantabSb,$=id=>document.getElementById(id); if(!sb)return;
const DINAS=['SUPER_ADMIN','KEPALA_DINAS','KABID','KASI_SD','KASI_SMP','SUBKOOR_TK','STAFF_DINAS','STAFF_TPG','PENGAWAS'];
const roleLabel={KEPALA_DINAS:'Kepala Disdikbud',KABID:'Kabid Ketenagaan',KASI_SD:'Kasi PPTK SD',KASI_SMP:'Kasi PPTK SMP',SUBKOOR_TK:'Subkoor PPTK TK/PAUD',STAFF_DINAS:'Staf Dinas',STAFF_TPG:'Staf TPG/Tamsil',PENGAWAS:'Pengawas'};
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const isDinas=()=>DINAS.includes(window.__simantabProfile?.role||'');
const isSuper=()=>window.__simantabProfile?.role==='SUPER_ADMIN'&&window.__simantabProfile?.is_admin===true;
function isSignup(){return !($('nameWrap')?.classList.contains('hidden'))}
function isDinasChannel(){return $('tabDinas')?.classList.contains('active')}
function authUi(){const email=$('email'),btn=$('authBtn'),hint=document.querySelector('.loginbox .hint');if(!email||!btn)return;const signup=isSignup(),dinas=isDinasChannel();if(dinas){email.type='text';email.autocomplete='username';const label=email.closest('.field')?.querySelector('label');if(label)label.textContent=signup?'Akun Dinas':'Username / Email';email.placeholder=signup?'Akun Dinas dibuat Super Admin':'contoh: agus atau email';if(signup){btn.disabled=true;btn.textContent='Akun dibuat Super Admin';if(hint)hint.innerHTML='<b>Akun Dinas dibuat oleh Super Admin.</b> Kasi, Subkoordinator, Staf, dan Pengawas menerima <b>username + password sementara</b>. Setelah login pertama, password wajib diganti melalui menu <b>Profil</b>.';}else{btn.disabled=false;btn.textContent='Masuk';if(hint)hint.innerHTML='<b>Login Dinas:</b> gunakan username sederhana atau email yang sudah terdaftar. Akun baru dari Super Admin memakai password sementara dan wajib menggantinya setelah login pertama.';}}else{email.type='email';email.autocomplete='email';const label=email.closest('.field')?.querySelector('label');if(label)label.textContent='Email';email.placeholder='nama@contoh.id';btn.disabled=false;btn.textContent=signup?'Daftar Akun':'Masuk';}}
const oldChannel=window.setChannel;if(oldChannel&&!window.__dinasLoginChannelWrap){window.__dinasLoginChannelWrap=true;window.setChannel=c=>{oldChannel(c);setTimeout(authUi,0)}}
const oldToggle=window.toggleAuthMode;if(oldToggle&&!window.__dinasLoginToggleWrap){window.__dinasLoginToggleWrap=true;window.toggleAuthMode=()=>{oldToggle();setTimeout(authUi,0)}}
const oldSubmit=window.submitAuth;if(oldSubmit&&!window.__dinasLoginSubmitWrap){window.__dinasLoginSubmitWrap=true;window.submitAuth=async()=>{const dinas=isDinasChannel(),signup=isSignup(),login=String($('email')?.value||'').trim(),password=String($('password')?.value||'');if(dinas&&signup){const msg=$('authMsg');msg.className='err';msg.textContent='Akun Dinas dibuat oleh Super Admin melalui menu Kelola Pengguna.';return}if(dinas&&!signup&&login&&!login.includes('@')){if(password.length<8){const msg=$('authMsg');msg.className='err';msg.textContent='Isi username dan password minimal 8 karakter.';return}const btn=$('authBtn'),msg=$('authMsg');btn.disabled=true;msg.className='small';msg.textContent='Memeriksa akun...';try{const {data,error}=await sb.functions.invoke('simantab-username-login',{body:{username:login,password}});if(error)throw error;if(!data?.access_token||!data?.refresh_token)throw new Error(data?.error||'Username atau password tidak sesuai.');const {error:setErr}=await sb.auth.setSession({access_token:data.access_token,refresh_token:data.refresh_token});if(setErr)throw setErr;location.reload()}catch(e){let detail='';try{detail=(await e?.context?.clone?.().json())?.error||''}catch(_){}msg.className='err';msg.textContent=detail||e?.message||'Username atau password tidak sesuai.';btn.disabled=false}return}return oldSubmit()}}
function tempPassword(username){const clean=String(username||'user').replace(/[^A-Za-z0-9]/g,'')||'User';let p=clean.charAt(0).toUpperCase()+clean.slice(1)+'@123';if(p.length<8)p+='4';return p}
function createForm(){if(!isSuper()||!$('usersBody')||$('dinasAccountCreator'))return;const box=document.createElement('div');box.id='dinasAccountCreator';box.className='card';box.style.marginBottom='12px';box.innerHTML=`<h3 style="margin-top:0">➕ Buat Akun Dinas</h3><div class="info" style="margin-bottom:10px">Buat akun dengan <b>username sederhana + password sementara</b>. Pengguna wajib mengganti password setelah login pertama.</div><div class="grid"><div class="field s6"><label>Nama lengkap</label><input id="newDinasName" placeholder="Nama pegawai"></div><div class="field s3"><label>Username</label><input id="newDinasUsername" autocomplete="off" placeholder="contoh: agus"></div><div class="field s3"><label>Password sementara</label><input id="newDinasPassword" autocomplete="new-password" placeholder="Agus@123"></div><div class="field s4"><label>Role</label><select id="newDinasRole">${Object.entries(roleLabel).map(([k,v])=>`<option value="${k}">${v}</option>`).join('')}</select></div><div class="field s4"><label>Unit</label><input id="newDinasUnit" value="Dinas Pendidikan dan Kebudayaan Kabupaten Batang"></div><div class="field s4"><label>Jabatan</label><input id="newDinasPosition" placeholder="Jabatan/unit kerja"></div></div><button class="btn primary" onclick="createDinasAccount()">Buat Akun</button><div id="newDinasMsg"></div>`;$('usersBody').prepend(box);const u=$('newDinasUsername'),p=$('newDinasPassword');u?.addEventListener('input',()=>{if(!p.dataset.manual)p.value=tempPassword(u.value)});p?.addEventListener('input',()=>p.dataset.manual='1')}
window.createDinasAccount=async()=>{if(!isSuper())return;const full_name=$('newDinasName').value.trim(),username=$('newDinasUsername').value.trim().toLowerCase(),password=$('newDinasPassword').value,role=$('newDinasRole').value,unit=$('newDinasUnit').value.trim(),position=$('newDinasPosition').value.trim(),msg=$('newDinasMsg');if(!full_name||!/^[A-Za-z0-9._-]{3,32}$/.test(username)||password.length<8){msg.className='err';msg.textContent='Isi nama, username 3–32 karakter, dan password sementara minimal 8 karakter.';return}msg.className='small';msg.textContent='Membuat akun...';const {data:{session}}=await sb.auth.getSession();const {data,error}=await sb.functions.invoke('simantab-admin-create-dinas-user',{body:{full_name,username,password,role,unit,position},headers:session?.access_token?{Authorization:`Bearer ${session.access_token}`}:{}});if(error||!data?.ok){let detail=data?.error||'';try{detail=detail||(await error?.context?.clone?.().json())?.error||''}catch(_){}msg.className='err';msg.textContent=detail||error?.message||'Gagal membuat akun.';return}msg.className='okmsg';msg.innerHTML=`<b>Akun berhasil dibuat.</b><br>Username: <b>${esc(username)}</b><br>Password sementara: <b>${esc(password)}</b><br><span class="small">Berikan kredensial ini kepada pengguna. Setelah login pertama, pengguna wajib mengganti password di Profil.</span>`;}
function credentialCard(){if(!isDinas()||!$('profileBody')||$('dinasCredentialCard'))return;const p=window.__simantabProfile||{},wrap=document.createElement('div');wrap.id='dinasCredentialCard';wrap.className='card';wrap.style.marginTop='12px';wrap.innerHTML=`<h3 style="margin-top:0">🔐 Username & Password Login</h3>${p.must_change_password?'<div class="notice" style="margin-bottom:10px"><b>Login pertama:</b> password sementara wajib diganti sebelum menggunakan menu lain.</div>':''}<div class="grid"><div class="field s6"><label>Username</label><input id="profileLoginUsername" value="${esc(p.login_username||'')}" placeholder="username"></div><div class="field s6"><label>Password baru</label><input id="profileNewPassword" type="password" autocomplete="new-password" placeholder="Minimal 8 karakter"></div><div class="field s6"><label>Ulangi password baru</label><input id="profileConfirmPassword" type="password" autocomplete="new-password" placeholder="Ulangi password"></div></div><button class="btn primary" onclick="saveDinasCredentials()">💾 Simpan Username / Password</button><div id="dinasCredentialMsg"></div>`;$('profileBody').appendChild(wrap)}
window.saveDinasCredentials=async()=>{if(!isDinas())return;const username=$('profileLoginUsername').value.trim().toLowerCase(),password=$('profileNewPassword').value,confirm=$('profileConfirmPassword').value,msg=$('dinasCredentialMsg'),p=window.__simantabProfile||{};if(!/^[A-Za-z0-9._-]{3,32}$/.test(username)){msg.className='err';msg.textContent='Username harus 3–32 karakter: huruf, angka, titik, garis bawah, atau tanda minus.';return}if(password&&password.length<8){msg.className='err';msg.textContent='Password baru minimal 8 karakter.';return}if(p.must_change_password&&!password){msg.className='err';msg.textContent='Pada login pertama, password baru wajib diisi.';return}if(password!==confirm){msg.className='err';msg.textContent='Ulangi password tidak sama.';return}msg.className='small';msg.textContent='Menyimpan...';const {data:{session}}=await sb.auth.getSession();const {data,error}=await sb.functions.invoke('simantab-account-settings',{body:{username,password:password||undefined},headers:session?.access_token?{Authorization:`Bearer ${session.access_token}`}:{}});if(error||!data?.ok){let detail=data?.error||'';try{detail=detail||(await error?.context?.clone?.().json())?.error||''}catch(_){}msg.className='err';msg.textContent=detail||error?.message||'Gagal menyimpan.';return}window.__simantabProfile={...p,...data.profile};msg.className='okmsg';msg.textContent='Username/password berhasil diperbarui.';setTimeout(()=>location.reload(),700)}
const oldShow=window.showTab;if(oldShow&&!window.__dinasLoginShowWrap){window.__dinasLoginShowWrap=true;window.showTab=async id=>{const p=window.__simantabProfile;if(p?.must_change_password&&DINAS.includes(p.role)&&id!=='profile')id='profile';await oldShow(id);await wait(30);if(id==='users')createForm();if(id==='profile')credentialCard()}}
for(let i=0;i<80&&!window.__simantabProfile;i++)await wait(100);
authUi();
if(window.__simantabProfile){if(window.__simantabProfile.must_change_password&&isDinas())await window.showTab('profile');else if(document.querySelector('.section.active')?.id==='profile')credentialCard();if(isSuper()&&document.querySelector('.section.active')?.id==='users')createForm()}
})();